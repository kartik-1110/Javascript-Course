const player = "Kartik";
const opponent = "Phil Ivey";
const game = "poker";
let points = 0;
let hasWon = false;

points += 100;
hasWon = true;
if (hasWon) {
  console.log(`${player} got ${points} points and won the ${game} game!`);
}
